# DeploymentPipeline Agent -- Few-Shot Examples

| # | User Request | Operation | CLI Command |
|---|---|---|---|
| 1 | "Create a DeploymentPipeline called X" | create | `fab deployment-pipeline create --display-name "X" --workspace-id "ws"` |
| 2 | "List all DeploymentPipeline items" | analyze | `fab deployment-pipeline list --workspace-id "ws"` |
| 3 | "Delete DeploymentPipeline abc" | delete | `fab deployment-pipeline delete --deployment-pipeline-id "abc"` |


---

# Execution Log (Auto-Appended Below)

