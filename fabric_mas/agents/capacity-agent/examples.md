# Capacity Agent -- Few-Shot Examples

| # | User Request | Operation | CLI Command |
|---|---|---|---|
| 1 | "Create a Capacity called X" | create | `fab capacity create --display-name "X" --workspace-id "ws"` |
| 2 | "List all Capacity items" | analyze | `fab capacity list --workspace-id "ws"` |
| 3 | "Delete Capacity abc" | delete | `fab capacity delete --capacity-id "abc"` |


---

# Execution Log (Auto-Appended Below)

